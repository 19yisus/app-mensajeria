import { api, helper } from "./helpers/persona";

beforeEach(() => {
    helper.precargarDatos()
})

afterEach(() => {
    helper.precargarDatos()
})


describe("test del modulo persona", () => {

    test("registro de persona con todos los datos", prueba => {

    })

})